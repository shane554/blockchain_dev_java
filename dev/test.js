const Blockchain = require('./blockchain');

// we now want tto make an instance of Blobkchain calles bitcoin
const bitcoin = new Blockchain;



console.log(bitcoin)






//console.log(bitcoin.chain[2]);
